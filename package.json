{
  "name": "pbai",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm -w @pbai/web run dev",
    "dev:api": "npm -w @pbai/api run dev",
    "dev:app": "npm run dev:web & npm run dev:api",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
